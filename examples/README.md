# Configuration Examples

Configuration guidance is now centralized in `docs/topics/configuration.md` to avoid duplication.

Use this folder only for concrete sample files if the team later decides to add them.

Current source-of-truth docs:

- `docs/topics/configuration.md`
- `docs/topics/quick-start.md`

